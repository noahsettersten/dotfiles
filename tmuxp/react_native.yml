session_name: react_native + rails
windows:
  - window_name: rails
    panes:
      - cd $RAILS_DIR && nvim
  # - window_name: console
  #   panes:
  #     - bundle exec rails console
  # - window_name: logs
  #   panes:
  #     - ./bin/dev
  - window_name: react native
    panes:
      - cd $RN_DIR && nvim
  - window_name: rn logs
    panes:
      - cd $RN_DIR && npm start
