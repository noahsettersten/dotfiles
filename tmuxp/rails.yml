session_name: rails
windows:
  - window_name: editor
    panes:
      - nvim
  - window_name: shell
    panes:
      - clear
  - window_name: console
    panes:
      - rails console
# - window_name: database
  # panes:
      # - psql
      # - mongo
  - window_name: webpack
    panes:
      - wph
  - window_name: logs
    panes:
      - bundle exec rails server
      - bundle exec sidekiq
      # - bundle exec sidekiq | tee ./log/sidekiq.log
