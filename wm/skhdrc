# A list of all built-in modifier and literal keywords can
# be found at https://github.com/koekeishiya/skhd/issues/1

# --- 1: App Shortcuts ---
# open terminal
alt - return : /Applications/Alacritty.app/Contents/MacOS/alacritty

# open qutebrowser
alt + shift - return : /Applications/qutebrowser.app/Contents/MacOS/qutebrowser
# ~/Scripts/qtb.sh

# open mpv
# cmd - m : open -na /Applications/mpv.app $(pbpaste)

# --- 2: Yabai Shortcuts ---
# 2a: Change window focus
alt - h : yabai -m window --focus west
alt - j : yabai -m window --focus south
alt - k : yabai -m window --focus north
alt - l : yabai -m window --focus east

# 2b: Move windows
hyper - h : yabai -m window --swap west
hyper - l : yabai -m window --swap east

# swap managed window
# shift + alt - h : yabai -m window --swap north

# move managed window
# shift + cmd - h : yabai -m window --warp east

# 2c: Change window size
shift + alt - h : yabai -m window --resize left:-20:0
shift + alt - l : yabai -m window --resize right:20:0

shift + alt - f : yabai -m config layout float
shift + alt - t : yabai -m config layout bsp

# hyper - h : yabai -m window --grid 1:2:0:0:1:1
# hyper - l : yabai -m window --grid 1:2:1:0:1:1
# hyper - j : yabai -m window --grid 1:4:1:0:2:1
# hyper - g : yabai -m window --grid 1:4:0:0:1:1
# hyper - ; : yabai -m window --grid 1:4:1:4:3:1
# hyper - b : yabai -m window --grid 2:4:1:0:1:1
hyper - f : yabai -m window --toggle zoom-fullscreen # zoom-parent

# My previous Rectangle shortcuts:
# - Hyper + h = left half
# - Hyper + l = rught half
# - Hyper + j = center in screen
# - Hyper + g = leftmost fourth
# - Hyper + ; = rightmost three-quarters
# - Hyper + f = fill screen / maximize
# - Hyper + b = second fourth




# balance size of windows
# shift + alt - 0 : yabai -m space --balance

# make floating window fill screen
# shift + alt - up     : yabai -m window --grid 1:1:0:0:1:1

# make floating window fill left-half of screen
# shift + alt - left   : yabai -m window --grid 1:2:0:0:1:1

# increase window size
# shift + alt - a : yabai -m window --resize left:-20:0
# shift + alt - w : yabai -m window --resize top:0:-20

# decrease window size
# shift + cmd - s : yabai -m window --resize bottom:0:-20
# shift + cmd - w : yabai -m window --resize top:0:20

# set insertion point in focused container
# ctrl + alt - h : yabai -m window --insert west

# toggle window split type
# alt - e : yabai -m window --toggle split

# float / unfloat window and center on screen
# alt - t : yabai -m window --toggle float --grid 4:4:1:1:2:2
