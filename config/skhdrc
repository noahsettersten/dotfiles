# A list of all built-in modifier and literal keywords can
# be found at https://github.com/koekeishiya/skhd/issues/1

# --- 1: App Shortcuts ---
alt - return : /Applications/Alacritty.app/Contents/MacOS/alacritty
hyper - return : /Applications/Alacritty.app/Contents/MacOS/alacritty
hyper - s: /Applications/Slack.app/Contents/MacOS/Slack
hyper - f: open /Applications/LibreWolf.app
# alt + shift - return : /Applications/qutebrowser.app/Contents/MacOS/qutebrowser

# --- 2: Yabai Shortcuts ---
# 2a: Change window focus
alt - h : yabai -m window --focus west
alt - j : yabai -m window --focus south
alt - k : yabai -m window --focus north
alt - l : yabai -m window --focus east

# 2b: Move windows
hyper - h : yabai -m window --swap west
hyper - j : yabai -m window --swap south
hyper - k : yabai -m window --swap north
hyper - l : yabai -m window --swap east

# 2c: Change window size
shift + alt - h : yabai -m window --resize left:-20:0
shift + alt - l : yabai -m window --resize right:20:0

# 2d: Switch between BSP and float layouts
shift + alt - f : yabai -m config layout float; yabai --message config focus_follows_mouse off
shift + alt - t : yabai -m config layout bsp; yabai --message config focus_follows_mouse autoraise

# 2e: Others
shift + alt - 0 : yabai -m space --balance # balance size of windows
alt - t : yabai -m window --toggle float --grid 4:4:1:1:2:2 # float / unfloat window and center on screen

# shift + alt - up     : yabai -m window --grid 1:1:0:0:1:1 # make floating window fill screen
# shift + alt - left   : yabai -m window --grid 1:2:0:0:1:1 # make floating window fill left-half of screen
# hyper - f : yabai -m window --toggle zoom-fullscreen # zoom-parent
